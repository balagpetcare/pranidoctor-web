# Web deployment — Phase 7

See `pranidoctor_user/docs/deployment/DEPLOY_RUNBOOK.md`.

- Validate env: `npm run validate:production-env`
- Health: `/api/health`, `/api/health/live`, `/api/health/ready`
- Legal pages served at `/privacy`, `/terms`, `/refund`
