# Template rich renderer (Flutter mobile)

This document lives in the **mobile** repo as the source of truth:

`pranidoctor_mobile/docs/template-rich-renderer.md`

See that file for the full specification (sanitization, widgets, admin alert classes, and dependencies).
