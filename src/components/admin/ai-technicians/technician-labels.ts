/**
 * AI technician admin copy + badge helpers.
 * Verification keys match `verificationSummary()` (shared with doctors).
 */
export {
  doctorVerificationBn,
  providerStatusBadgeVariant,
  providerStatusBn,
  userStatusBadgeVariant,
  userStatusBn,
  verificationBadgeVariant,
} from "../doctors/doctor-labels";
